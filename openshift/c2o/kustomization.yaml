apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - services.yaml
  - routes.yaml

namespace: c2o

commonLabels:
  app: c2o
  component: claude-workspace
